cmake_minimum_required(VERSION 3.10)
project(hnet VERSION 1.0)

# Add subdirectories
add_subdirectory(lib)
add_subdirectory(src)
# If you have tests in the 'tests' directory, uncomment the following line
# add_subdirectory(tests)
